apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: librum-db-pvc
  namespace: librum
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  volumeName: "librum-db-pv"